# Main Hydra configuration entrypoint
# Composes all sub-configs using Hydra defaults

defaults:
  - paths: default
  - training: default
  - model: default
  - data: default
  - logging: mlflow
  - _self_

# Global settings
seed: 42
